@graph{
  @title{Lógica en componentes Svelte}
}

@text[[[

Logic
@ulist{
  @row{@code{if} blocks}
  @row{@code{else} blocks}
  @row{@code{else if} blocks}
  @row{@code{each} blocks
    @ulist{
      @{Keys}
    }
  }
  @row{@code{await} blocks}
}

]]]